@using PatientPortal.Extensions
@using Microsoft.AspNetCore.Authorization
@using PatientPortal.Authorization
@inject IAuthorizationService AuthorizationService

@{
    var isAuthenticated = User.Identity?.IsAuthenticated ?? false;
    var canManageStaff = isAuthenticated && (await AuthorizationService.AuthorizeAsync(User, PolicyNames.ManageStaff)).Succeeded;
    var canManagePatients = isAuthenticated && (await AuthorizationService.AuthorizeAsync(User, PolicyNames.ManagePatients)).Succeeded;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - PatientPortal</title>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" />
    <script src="https://kit.fontawesome.com/bb93b9a982.js" crossorigin="anonymous"></script>
</head>
<body class="bg-light overflow-hidden">
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow">
            <div class="container">
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <a class="navbar-brand text-nowrap" asp-area="" asp-controller="Home" asp-action="Index">PatientPortal</a>
                @if(isAuthenticated)
                {
                    <div class="order-0 order-sm-last">
                        <a asp-controller="Messaging" asp-action="Inbox"><i class="fas fa-envelope fa-2x"></i></a>
                    </div>
                }
                <div class="navbar-collapse collapse d-sm-inline-flex">
                    @if(isAuthenticated)
                    {
                        <h6 class="p-2 text-dark">Welcome, @User.GetFullName()!</h6>
                    }
                    <ul class="navbar-nav flex-grow-1 order-sm-first">
                        <li class="nav-item border-left border-right border-muted">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                        </li>
                        @if(isAuthenticated)
                        {
                            @if(canManageStaff)
                            {
                                <li class="nav-item">
                                    <a class="nav-link text-dark" asp-area="" asp-controller="Staff"
                                        asp-action="StaffManager">Staff</a>
                                </li>
                            }
                            @if(canManagePatients)
                            {
                                <li class="nav-item">
                                    <a class="nav-link text-dark" asp-area="" asp-controller="Patients"
                                        asp-action="PatientManager">Patients</a>
                                </li>
                            }
                        }
                        @if(!isAuthenticated)
                        {
                            <li class="nav-item">
                                <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="About">About</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
                            </li>
                        }
                    </ul>
                     @if(isAuthenticated)
                    {
                        <form class="p-2" asp-controller="Login" asp-action="Logout">
                            <button class="btn btn-secondary btn-sm">Logout</button>
                        </form>
                    }
                </div>
            </div>
        </nav>
    </header>
    <div class="container main-container p-0 bg-white box-shadow">
        <main class="px-3 pb-3" role="main">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted bg-white mt-1">
        <div class="container">
            &copy; 2021 - 2026 - PatientPortal 
            - <a asp-area="" asp-controller="Home" asp-action="About">About</a> 
            - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
