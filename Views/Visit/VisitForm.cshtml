@model VisitFormView

<partial name="~/Views/Shared/_PatientInfoHeader.cshtml" for="Patient"/>

<h3>New Visit: </h3>
<form asp-action="VisitCreate" asp-controller="Visit" asp-route-patientId="@Model.Patient.CurrentPatientId" method="post">
    <div class="form-group">
        <label class="form-label" asp-for="Visit.DateOfVisit"></label>
        <span class="text-danger" asp-validation-for="Visit.DateOfVisit"></span>
        <input class="form-control" asp-for="Visit.DateOfVisit">
    </div>
    <div class="form-group">
        <label class="form-label" asp-for="Visit.Comment"></label>
        <span class="text-danger" asp-validation-for="Visit.Comment"></span>
        <textarea class="form-control" asp-for="Visit.Comment"></textarea>
    </div>
    <div class="form-group">
        @for(int i = 0; i < @Model.HealthIssues.Count; i++)
        {
        <div class="form-check">
            <input class="form-check-input" type="checkbox" asp-for="HealthIssues[i].Selected">
            <label class="form-check-label" asp-for="HealthIssues[i].Selected">
                @Model.HealthIssues[i].ShortDescription
            </label>
            <input class="form-check-input" type="hidden" asp-for="@Model.HealthIssues[i].HealthIssueId">
            <input class="form-check-input" type="hidden" asp-for="@Model.HealthIssues[i].ShortDescription">
        </div>
        }
    </div>
    <div class="form-group">
        <label class="form-label" for="Upload"></label>
        <input class="form-control"type="file" name="Upload" id="Upload">
    </div>
    <button class="btn btn-info">Add Visit Note</button>
    <a class="btn btn-outline-danger" asp-controller="Patients" asp-action="PatientInfo" asp-route-patientId="@Model.Patient.CurrentPatientId">Cancel</a>
</form>